<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kanika">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.RowModel"></activity>
        <activity android:name=".View.Upload2Activity" />
        <activity android:name=".View.DetailActivity" />
        <activity android:name=".View.ItemsActivity" />
        <activity android:name=".View.shareholderActivity" />
        <activity android:name=".View.UploadActivity" />
        <activity android:name=".View.UserDashboard" />
        <activity android:name=".View.AdminActivity" />
        <activity android:name=".View.SignupActivity" />
        <activity android:name=".View.LoginActivity" />
        <activity
            android:name=".View.MainActivity"
            android:theme="@style/FirstTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>